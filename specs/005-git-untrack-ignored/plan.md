# Implementation Plan: Git Untrack Ignored Files

**Branch**: `005-git-untrack-ignored` | **Date**: 2026-04-23 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/005-git-untrack-ignored/spec.md`
**User argument**: "I want to make sure no output folder is tracked by accident after this spec is implemented. Any folder that is already tracked and should not be needs to be untracked."

## Summary

Fix the repository's git tracking state: remove genuinely unwanted files from the index (`.DS_Store`), narrow the `.vscode/` ignore rule to preserve the project-level `settings.json`, and remove the erroneous `frontend/tests/` gitignore rule that incorrectly marks source test files as ignored. Output folders (`playwright-report/`, `test-results/`, `.next/`, `out/`, `node_modules/`) are confirmed already untracked and covered by existing rules — no action needed for them.

## Technical Context

**Language/Version**: N/A — git repository hygiene only
**Primary Dependencies**: git (existing)
**Storage**: N/A
**Testing**: `git ls-files -ci --exclude-standard` (returns empty), `git ls-files` (confirms test files tracked)
**Target Platform**: All contributor machines (macOS primary)
**Project Type**: Repository maintenance
**Performance Goals**: N/A
**Constraints**: `.vscode/settings.json` must remain tracked — it contains project-level Speckit configuration. `.DS_Store` must be untracked. Source test files must remain tracked. All output folders must remain untracked.
**Scale/Scope**: 1 `.gitignore` file, 2 tracked files to remove, 1 gitignore rule to remove, 1 gitignore rule to add

## Constitution Check

_GATE: Must pass before Phase 0 research. Re-check after Phase 1 design._

| Principle                          | Status | Notes                                                           |
| ---------------------------------- | ------ | --------------------------------------------------------------- |
| I. Static-First Delivery           | N/A    | No application code changed                                     |
| II. Accessible By Default          | N/A    | No UI changes                                                   |
| III. Responsive Core Experience    | N/A    | No UI changes                                                   |
| IV. Minimal Client-Side Complexity | N/A    | No JS added                                                     |
| V. Content And Link Integrity      | PASS   | No pages, links, or assets changed; source test files preserved |

**Post-Phase-1 Constitution Check**: PASS — change is purely to git index and `.gitignore`.

## Project Structure

### Documentation (this feature)

```text
specs/005-git-untrack-ignored/
├── plan.md              # This file
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output
├── quickstart.md        # Phase 1 output
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Files changed (repository root)

```text
.gitignore              # EDIT: remove frontend/tests/ rule; add !.vscode/settings.json negation
                        # REMOVE duplicate .vscode/ and *.log/*.swp entries
git index               # git rm --cached .DS_Store
```

**Structure Decision**: Single-project change. No source files created or modified. Changes are to `.gitignore` (one file) and the git index (one `git rm --cached` command).

## Complexity Tracking

> No Constitution violations. Section not applicable.
