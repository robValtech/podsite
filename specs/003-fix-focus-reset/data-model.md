# Data Model: Fix Focus Reset

This feature does not introduce or modify any data entities. It is a client-side behavior fix that resets keyboard focus after route changes using existing DOM elements (`document.body`).

No new types, schemas, or content structures are required.
